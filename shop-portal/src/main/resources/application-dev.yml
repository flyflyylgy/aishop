# 开发 profile：java -jar --spring.profiles.active=dev
# 仅用于本地开发，提供默认密码。生产环境必须使用 prod profile 并设置环境变量。
spring:
  datasource:
    password: ${MYSQL_PASSWORD:root123456}

shop:
  jwt:
    secret: ${JWT_SECRET:dev-only-please-change-me-in-production-0123456789}
